node_modules
extra
dist
release
out
.DS_Store
*.log*
.idea
*.ttf
# The emoji font IS shipped in-repo (main.css hard-references it; the CDN
# fallbacks in prepare.mjs are unreliable). Un-ignore it explicitly.
!src/renderer/src/assets/NotoColorEmoji.ttf
party.md
CLAUDE.md
agent.md
tsconfig.node.tsbuildinfo
AGENTS.md
.codegraph
node_modules/.temp
deploy/gateway/.env
deploy/gateway/.env.bak
deploy/gateway/gateway.db
deploy/gateway/gateway.db.backup
deploy/gateway/mihomo/providers
*.cpx
*.db-*
# local AI-assistant spec documents
.trae
# local AI-assistant / IDE config
.codebuddy
.codebelly
.vscode

# local gateway test data (SQLite db from source-code runs)
deploy/gateway/data

# machine-local compose overrides (host network mode: ports gate no longer needed)
deploy/gateway/docker-compose.override.yml
deploy/gateway/docker-compose.override.yml.bak-*

# mihomo-lines tool: personal host + party web token (keep the .example only)
tools/mihomo-lines/scripts/lines.config.json

# locally built source tarball for deploy/opt/bootstrap.sh
cpx-src.tar.gz
